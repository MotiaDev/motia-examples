Create a Mermaid component diagram for this repository.

Repository: {{repoUrl}}
Key Files: {{keyFiles}}
Language Breakdown: {{languageBreakdown}}

Generate a Mermaid graph showing:
1. Main modules/components
2. Their relationships
3. Dependencies between files/modules
4. Configuration files

Use Mermaid graph syntax (graph TD or LR).
Only return the Mermaid code, no explanations.
